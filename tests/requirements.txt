# Dependencies for integration tests.
# Pinned to reasonably recent stable versions; CI should install via:
#   pip install -r tests/requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-timeout==2.3.1
testcontainers==4.8.2
paho-mqtt==2.1.0
httpx==0.27.2
websockets==13.1
